<h1>{{albumName}}: Tracks</h1>

{{#each albumTracks}}
<p>{{this.name}}</p>
            <audio controls>
                <source src="{{this.preview_url}}" type="audio/mpeg">
            </audio>
{{/each}}